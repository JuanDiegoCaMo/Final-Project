#include "prota.h"

prota::prota(int width, int height)
{
    /*for(int i=0; i<8; i++){
     *  original[i].load();
     * }
     * w = width;
     * h = height;
     * setSize(w,h);
    */
}

void prota::setSize(int width, int height)
{

}
